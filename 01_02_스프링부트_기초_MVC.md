# Spring Boot 1강 ~ 2강 정리

## 1. Spring Boot 프로젝트 생성

### 실습 환경
- IDE: IntelliJ IDEA
- Language: Java
- Build Tool: Gradle - Groovy
- JDK: 17
- Spring Boot: 3.x
- Dependencies
  - Spring Web
  - Mustache
  - Spring Data JPA
  - H2 Database

---

## 2. MVC 패턴

MVC(Model-View-Controller)는 역할을 분리하여 프로그램을 관리하는 구조이다.

### Model
- 데이터를 저장하고 전달하는 역할
- Controller → View 로 데이터 전달 시 사용

### View
- 사용자에게 보여지는 화면(UI)
- Mustache 템플릿 엔진 사용
- `templates` 폴더에 생성

### Controller
- 사용자의 요청(URL)을 처리
- 어떤 View를 보여줄지 결정

### MVC 흐름

```text
사용자 요청 → Controller → Model(데이터 저장) → View → 화면 출력
```

---

## 3. Controller 만들기

Controller 클래스에는 `@Controller` 어노테이션을 사용한다.

```java
@Controller
public class FirstController {

}
```

### URL 요청 처리

`@GetMapping()`을 사용하여 URL 요청을 처리한다.

```java
@GetMapping("/hi")
public String hello() {
    return "greetings";
}
```

### 동작 과정
1. 브라우저에서 `/hi` 요청
2. Controller 실행
3. `"greetings"` 반환
4. `templates/greetings.mustache` 실행

---

## 4. Mustache 템플릿

Mustache는 View 템플릿 엔진이다.

### 위치

```text
src/main/resources/templates
```

### 파일 생성 예시

`greetings.mustache`

```html
<h1>Hello</h1>
```

Controller:

```java
@GetMapping("/hi")
public String hello() {
    return "greetings";
}
```

---

## 5. Model로 데이터 전달

Controller에서 View로 데이터를 넘길 때 `Model` 사용

### Controller

```java
@GetMapping("/hi")
public String niceToMeetYou(Model model) {
    model.addAttribute("username", "홍길동");
    return "greetings";
}
```

### Mustache

```html
<h1>{{username}}</h1>
```

### 출력 결과

```text
홍길동
```

### addAttribute 의미

```java
model.addAttribute("key", value);
```

- key: View에서 사용할 이름
- value: 전달할 실제 데이터

예시:

```java
model.addAttribute("username", "홍길동");
```

→ `username` 이름으로 `"홍길동"` 전달

---

## 6. 핵심 정리

### 필수 어노테이션
- `@Controller`
- `@GetMapping()`

### 필수 개념
- MVC 패턴
- View Template(Mustache)
- Model 데이터 전달

### 핵심 문법

```java
@GetMapping("/주소")
```

```java
return "파일이름";
```

```java
model.addAttribute("이름", 값);
```

```mustache
{{변수명}}
```

---

## 느낀 점
MVC 패턴은 역할을 나누어 관리하기 쉽게 만든 구조라는 것을 알게 되었다. Controller가 요청을 처리하고, Model이 데이터를 전달하며, View가 화면을 출력하는 흐름을 이해했다. 또한 Mustache를 통해 데이터를 화면에 출력하는 방법을 배웠다.
